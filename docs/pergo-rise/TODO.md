# TODO / External Validation

These items are not silently treated as complete:

- Validate `Shape005` against the authoritative Pergo Rise accessory/CAD catalog before enabling production geometry.
- Validate semantic roles of `Shape274`, `Shape254` and `Shape117` against authoritative profile names.
- Run physical Android Chrome + ARCore + HTTPS immersive-ar placement and metre comparison.
- Where production BOM is later required, consume only components marked `productionGeometry=true`; static fabric and review-only connection candidates remain excluded.
